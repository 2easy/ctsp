#!/usr/bin/env python
from greedy import greedy_solution
from random import randint

def mix(t1, t2, solution):
    # TODO mix 6 elements excluding current ones

def neighbourhood(solution):
    t1,t2 = sample(range(0,len(solution)))
    return mix(t1, t2, solution)
